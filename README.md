Hi

⚠️ **Temporary Coming Soon Mode** — The original portfolio is preserved. See `🔄 Restore Portfolio` below to bring it back.

## 🔄 Restore Portfolio

The current `index.html` is a temporary Coming Soon page. The original portfolio has been safely preserved and must NOT be deleted.

**Where is the original portfolio?**
- Portfolio homepage: `portfolio.html` (your original `index.html`)
- Portfolio styles: `portfolio.css` (your original `style.css`)

**Which file is live right now?**
- `index.html` — the temporary Coming Soon page
- `style.css` — the temporary Coming Soon styles

**To restore your portfolio:**

1. Copy `portfolio.html` over `index.html`
2. Copy `portfolio.css` over `style.css`
3. Verify locally by opening `index.html` in a browser
4. Commit the changes:
   ```bash
   git add index.html style.css portfolio.html portfolio.css
   git commit -m "Restore original portfolio homepage"
   ```
5. Push to GitHub:
   ```bash
   git push origin main
   ```

After pushing, your original portfolio will be live again.

⚠️ **Do NOT delete `portfolio.html` or `portfolio.css` until the portfolio has been successfully restored and verified.**